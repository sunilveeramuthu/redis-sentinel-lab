Convert the flatDir repository for the vendored ffb/4fb jars in libs/ (currently `flatDir { dirs 'libs' }`) into an Ivy repository using patternLayout, matching the actual jar filenames in libs/ (e.g. `[artifact]-[revision].[ext]` — adjust to real naming, inspect libs/ first). Use `metadataSources { artifact() }` since there's no ivy.xml/pom. Wrap it in `exclusiveContent { forRepository { ... } filter { includeGroup("com.fourbusiness") } }` so Gradle never scans this repo for non-4fb dependencies and never scans remote repos (mavenCentral, Artifactory) for 4fb group artifacts. If the pattern can't encode an organisation/group token, use includeModule(group, artifactName) per jar instead of includeGroup. Flag any jars in libs/ that appear to have inter-dependencies on each other, since artifact()-only metadata means no transitive resolution — list them rather than silently dropping the dependency.
